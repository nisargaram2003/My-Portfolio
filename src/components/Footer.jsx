import React from 'react';

function Footer() {
  return (
    <footer className="footer">
      &copy; {new Date().getFullYear()} Portfolio — Built using React 
    </footer>
  );
}

export default Footer;
